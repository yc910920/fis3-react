/**
 * @file reducers.es6
 * @email 99874405@qq.com
 */
export default {
    reducers: {
        A(state = 'a', action) {
            return state;
        },
        B(state = 'b', action) {
            return state;
        },
        C(state = 'c', action) {
            return state;
        }
    }
};